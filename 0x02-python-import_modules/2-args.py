#!/usr/bin/pythion3
if __name__ = "__main__":
        import sys

        if arg == 0:
                print("{} arguments".format(arg))
            elif arg == 1:
                print("{} argument".format(arg))
            else:
                print("{} arguments: ".format(arg))

            for j in range(arg):
                print("{} : {}".format(j+1, sys.arg(i+1))
